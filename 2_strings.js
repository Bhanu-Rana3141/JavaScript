/*

const name = "Bhanu Rana"
const repoCount = 10

// console.log(name + repoCount + " value"); BAD PRACTICE

// STRING LITERALS
console.log(`my name is ${name} and repo count is ${repoCount}`);
// ${name}: This is a placeholder within the template literal denoted by ${}. It allows you to embed the value of the variable name within the string. The value of name will be substituted into the string at this position.

// negative indexing is not possible in substring , if used indexing will start from 0

const nameReplace = name;
console.log(nameReplace.replace(' ', '-'))

*/

let s = "hello my name is bhanu partap singh rana"
console.log(s);
console.log(s.substring(0, 5));